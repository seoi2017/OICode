//HDU-2449[Unaccepted]
//Tag:[高斯消元（线性代数）][高精度（技巧与方法）]
//Code By HYS.
//--ms,-.-MB(----KB),----B
#include<iostream>
#include<cstdio>
#include<cstring>
#include<cstdlib>
#include<iostream>
#include<cmath>
#define MX
#define MX_SIZE
using namespace std;
struct bigint_t{
    int fz[MX_SIZE],fm[MX_SIZE];

};
int main(){
#ifdef LOCAL
    freopen("io/in","r",stdin);
    freopen("io/out","w",stdout);
#endif
    while(scanf("%d",&n)!=EOF){
        scanf();
    }
#ifdef LOCAL
    fclose(stdin);
    fclose(stdout);
#endif
    return 0;
}